#include <stdio.h>
#include <string.h>

struct cel{
    int conteudo;
    struct cel*seg;
}
int main()
{
    char nomes1[50], nomes2[50], nomes3[50];
    char *novo;

    scanf("%s, %s, %s", &nomes1, &nomes2, &nomes3);
    

    cel *lst;
    lst = NULL;

return 0;
}
